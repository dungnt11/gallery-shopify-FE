/**
 * Generate effect - 028
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect28(effect, imageDOM) {
	return `<figure class="effect-ming">
    ${imageDOM}
    <figcaption>
      <h2>Funny <span>Ming</span></h2>
      <p>Ming sits in the corner the whole day. She's into crochet.</p>
    </figcaption>			
  </figure>`;
}

export { effect28 };
