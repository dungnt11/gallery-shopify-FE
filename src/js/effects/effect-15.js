/**
 * Generate effect - 15
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect15(effect, imageDOM) {
	return `<figure class="effect-milo">
    ${imageDOM}
    <figcaption>
      <h2>Faithful <span>Milo</span></h2>
      <p>Milo went to the woods. He took a fun ride and never came back.</p>
      <a href="#">View more</a>
    </figcaption>			
  </figure>`;
}

export { effect15 };
