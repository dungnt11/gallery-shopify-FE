/**
 * Generate effect - 14
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect14(effect, imageDOM) {
	return `<figure class="effect-chico">
    ${imageDOM}
    <figcaption>
      <h2>Silly <span>Chico</span></h2>
      <p>Chico's main fear was missing the morning bus.</p>
    </figcaption>			
  </figure>`;
}

export { effect14 };
