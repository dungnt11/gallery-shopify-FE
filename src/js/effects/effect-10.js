/**
 * Generate effect - 10
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect10(effect, imageDOM) {
	return `<figure class="10 effect-bubba">
    ${imageDOM}
    <figcaption>
      <h2>Fresh <span>Bubba</span></h2>
      <p>Bubba likes to appear out of thin air.</p>
    </figcaption>			
  </figure>`;
}

export { effect10 };
