/**
 * Generate effect - 29
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect29(effect, imageDOM) {
	return `<figure class="effect-lexi">
    ${imageDOM}
    <figcaption>
      <h2>Altruistic <span>Lexi</span></h2>
      <p>Each and every friend is special. Lexi won't hide a single cookie.</p>
    </figcaption>			
  </figure>`;
}

export { effect29 };
