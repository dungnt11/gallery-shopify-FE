/**
 * Generate effect - 011
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect11(effect, imageDOM) {
	return `<figure class="effect-romeo">
    ${imageDOM}
    <figcaption>
      <h2>Wild <span>Romeo</span></h2>
      <p>Romeo never knows what he wants. He seemed to be very cross about something.</p>
    </figcaption>			
  </figure>`;
}

export { effect11 };
