/**
 * Generate effect - 29
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect30(effect, imageDOM) {
	return `<figure class="effect-duke">
  ${imageDOM}
    <figcaption>
      <h2>Messy <span>Duke</span></h2>
      <p>Duke is very bored. When he looks at the sky, he feels to run.</p>
    </figcaption>			
  </figure>`;
}

export { effect30 };
