/**
 * Generate effect - 020
 * @param effect
 * @param imageDOM -  image DOM
 */
function effect20(effect, imageDOM) {
	return `<figure class="effect-selena">
    ${imageDOM}
    <figcaption>
      <h2>Happy <span>Selena</span></h2>
      <p>Selena is a tiny-winged bird.</p>
    </figcaption>			
  </figure>`;
}

export { effect20 };
